const initialState = {
    favorite: {
        companies: [],
    },
};

const mainReducer = (currentState = initialState, action) => {
    switch (action.type) {
        default:
            return currentState;
    }
};

export default mainReducer;
